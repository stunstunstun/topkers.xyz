{
  "name": "blahblah-integration",
  "version": "0.1.1",
  "main": "./dist/index",
  "scripts": {
    "clean": "rimraf eslint build .cache coverage checkstyle*.xml *.log",
    "lint": "eslint . --ignore-path .eslintignore",
    "lint:html": "eslint . --ignore-path .eslintignore -f html -o eslint/index.html",
    "lint:checkstyle": "eslint . --ignore-path .eslintignore -f checkstyle > checkstyle-result.xml",
    "lint:report": "yarn clean && yarn lint:html && yarn lint:checkstyle",
    "test": "jest",
    "build": "babel src -d dist --presets env",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "author": "stunstunstun",
  "license": "MIT"
}
